"""LLM provider adapters (OpenAI-compatible HTTP).

Populated by the SKY-57 provider-abstraction commit: ``base`` protocol,
``openai_compatible`` adapter, per-provider presets and the local-only data
residency registry.
"""
