"""Pure domain layer: entities and value objects with no framework imports.

Populated as AI feature slices introduce domain concepts (suggestion state
machine, anomaly lifecycle). Kept separate from ORM models by design.
"""
